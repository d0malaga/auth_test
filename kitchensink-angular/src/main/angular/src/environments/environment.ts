export const environment = 
{
    production: false,
    // If the UI is deployed somewhere else, like using 'ng serve',
    // then the browser cannot call the API unless the server add CORS
    API_URL: 'http://localhost:8080/kitchensink-angular/rest/',
};

